{{ define "index" }}
  <!DOCTYPE html>
  <html lang="en">
    {{ template "head" . }}
    <body>
      <script
        src="{{ RootResolver .Config.StaticDir `index.js` }}"
        defer
      ></script>
      <div class="index-layout">
        <header class="index-header">
          <h1 id="site-title">
            {{ .Config.Title }}
          </h1>
          <button id="viewmode-toggler" class="noscript">
            {{- .Config.DefaultViewMode -}}
          </button>
        </header>
        <div id="info">
          {{ if .Config.ThemeConfig.bio }}
            <p class="bio">
              {{ RenderNewline .Config.ThemeConfig.bio }}
            </p>
            <div class="pills">
              <span> {{ .Photos.Len }} photos </span>
              {{ if .Config.ThemeConfig.links }}
                <span>·</span>
                {{ range $name, $href := .Config.ThemeConfig.links }}
                  <a href="{{ $href }}" class="pill">{{ $name }}↗</a>
                {{ end }}
              {{ end }}
            </div>
          {{ end }}
        </div>
        <div
          id="thumbs"
          class="{{- if eq .Config.DefaultViewMode `gallery` -}}
            thumbs-gallery
          {{- else -}}
            thumbs-grid
          {{- end -}}"
        >
          {{ range $i, $id := .Photos.IDs }}
            {{ $img := $.Photos.Get $id }}
            {{ $idxp := IndexPhoto $img $i }}
            {{ template "thumb" $idxp }}
          {{ end }}
        </div>
      </div>
    </body>
  </html>
{{ end }}
